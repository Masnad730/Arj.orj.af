/** @type {import('next').NextConfig} */
const nextConfig = {
  // i18n: {
  //   locales: ['en-US', 'fa-AF'],
  //   defaultLocale: 'en-US',
  // },
  trailingSlash: true,
}

module.exports = nextConfig
